# AERO Launcher release channel

Public update channel for the shared AERO Launcher core.

Stable Windows builds are published in GitHub Releases. Each stable release
contains one required asset named `AeroLauncher.exe`. Published assets are not
replaced; every update receives a new semantic version tag.
